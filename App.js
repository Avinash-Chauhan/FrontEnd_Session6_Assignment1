//declared variable radius r using var keyword and assigned value 2 to it
var r=2;
//declared variable Area a and applied formula of area of circle 
var a=Math.PI*r*r;
//printing result in console as output 
console.log("Area of Circle is "+a);